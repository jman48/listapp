== Listapp-be

This server application syncs and saves all lists, items and users for the listapp-mobile android app. It uses a postgres database to store all data.

See listapp-mobile for the android application that uses this web service.
